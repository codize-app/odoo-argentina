<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record model="ir.ui.view" id="account_check_action_wizard_form_view">
        <field name="name">account.check.action.wizard.form</field>
        <field name="model">account.check.action.wizard</field>
        <field name="arch" type="xml">
            <form string="Acción de Cheque">
                <field name="action_type" invisible="True"/>
                <group >
                    <field name="date"/>
		            <field name="journal_id" invisible = "action_type != 'bank_deposit'" domain="[('type','=','bank')]"/>
                    <field name="reference"/>
                    <field name="grouped_entry" widget="boolean_toggle" string="Asiento Agrupado"/>
                </group>
                <footer>
                    <button string="Confirm" name="action_confirm" type="object" class="oe_highlight"/>
                    o
                    <button string="Cancel" class="oe_link" special="cancel"/>
                </footer>     
            </form>
        </field>
    </record>

    <record id="action_account_check_action_wizard" model="ir.actions.act_window">
        <field name="name">Acción de Cheque</field>
        <field name="res_model">account.check.action.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>

</odoo>
