<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="exe_view_move_form_debit_fix" model="ir.ui.view">
        <field name="name">exe.account.move.form.debit.fix</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">

            <xpath expr="//button[@name='action_reverse']" position="after">
                <button name="account_debit_note.action_view_account_move_debit" string="Agregar Nota Débito" type="action" groups="account.group_account_invoice" invisible="move_type not in ('out_invoice', 'in_invoice', 'out_refund', 'in_refund') or state != 'posted' or debit_origin_id != False"/>
            </xpath>

        </field>
    </record>
</odoo>
