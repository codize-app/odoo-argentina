<?xml version="1.0" encoding="utf-8"?>
<odoo>

<!-- Journal Items base list view -->
<record id="view_move_line_tree" model="ir.ui.view">
    <field name="name">account.move.line.tree</field>
    <field name="model">account.move.line</field>
    <field name="priority">99</field>
    <field name="arch" type="xml">
        <list string="Journal Items">
            <field name="date"/>
            <field name="date_maturity"/>
            <field name="move_id"/>
            <field name="journal_id" options='{"no_open": True}'/>
            <field name="name"/>
            <field name="ref"/>
            <field name="account_id"
                   options='{"no_open": True}'
                   domain="[]"/>
            <field name="balance" string="Amount"/>
            <field name="amount_residual" sum="Total"/>
            <field name="amount_currency" groups="base.group_multi_currency"/>
            <field name="amount_residual_currency" groups="base.group_multi_currency"/>
            <field name="currency_id" column_invisible="1"/>
            <field name="company_currency_id" column_invisible="1"/>
            <field name="company_id" column_invisible="1"/>
        </list>
    </field>
</record>

<!-- Journal Items with payment group matched amount -->
<record id="view_move_line_with_matched_tree" model="ir.ui.view">
    <field name="name">account.move.line.tree.matched</field>
    <field name="model">account.move.line</field>
    <field name="priority" eval="99"/>
    <field name="mode">primary</field>
    <field name="inherit_id" ref="account_payment_group.view_move_line_tree"/>
    <field name="arch" type="xml">
        <field name="amount_residual" position="replace">
            <field name="payment_group_matched_amount" string="Monto Pago" sum="Total"/>
        </field>
        <field name="amount_residual_currency" position="replace">
            <field name="financial_amount_residual" sum="Total" groups="account_financial_amount.account_use_financial_amounts"/>
        </field>
    </field>
</record>

<!-- Override register payment button on account.move form -->
<record id="view_move_form_payment_group" model="ir.ui.view">
    <field name="name">account.move.payment.group</field>
    <field name="model">account.move</field>
    <field name="inherit_id" ref="account.view_move_form"/>
    <field name="arch" type="xml">
        <button name="action_register_payment" position="attributes">
            <attribute name="name">action_account_invoice_payment_group</attribute>
            <attribute name="type">object</attribute>
        </button>
    </field>
</record>

</odoo>
